# 开发环境默认用 SQLite，无需安装 PostgreSQL。文件会生成在 prisma/dev.db
DATABASE_URL="file:./prisma/dev.db"
# Railway（SQLite + 持久化卷）示例：
# DATABASE_URL="file:/data/dev.db"
# 若使用 PostgreSQL，改为：postgresql://用户名:密码@localhost:5432/数据库名?schema=public
# 32 字节 base64 密钥（可选）。生成：node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=""
# 生产环境建议配置：允许前端域名（可逗号分隔多个域名）
# CORS_ORIGIN="https://your-web.vercel.app"

# 可选：Binance 合约接口网络配置（当前网络无法访问 fapi.binance.com 时使用）
# BINANCE_FAPI_PUBLIC_URL="https://fapi1.binance.com/fapi/v1"
# BINANCE_FAPI_PRIVATE_URL="https://fapi1.binance.com/fapi/v1"
# BINANCE_HTTPS_PROXY="http://127.0.0.1:7890"
